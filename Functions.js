//Functions will go here!!!! (export function 'example'()then in Index type (import {example} from './Functions.js'))

function CreateMonster(){let R = math.floor(Math.random() * 3) + 1;
MonsterHP: 30,
MonsterLevel: (R % 3) +,
MonsterXP: (MonsterHP/2),
if (MonsterHP <= 10) {
  CreateMonster();
};
if (MonsterLevel ==0) {
  CreatMonster();
}