# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
art-staging/
# Published by `node tools/art/bake.mjs` out of art-staging: the normal and
# albedo maps the renderer samples, put where Vite can copy them into dist.
# Derived, regenerated byte-for-byte by the baker, ~54 MB. Not source.
public/art/materials/

# API key — never commit
.elevenlabs.key
gear-staging/
gen2-staging/
